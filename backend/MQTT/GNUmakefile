# This file was automatically generated by bakefile.
#
# Any manual changes will be lost if it is regenerated,
# modify the source .bkl file instead if possible.

# You may define standard make variables such as CFLAGS or
# CXXFLAGS to affect the build. For example, you could use:
#
#      make CXXFLAGS=-g
#
# to build with debug information. The full list of variables
# that can be used by this makefile is:
# AR, CC, CFLAGS, CPPFLAGS, CXX, CXXFLAGS, LD, LDFLAGS, MAKE, RANLIB.

# You may also specify config=Debug|Release
# or their corresponding lower case variants on make command line to select
# the corresponding default flags values.
ifeq ($(config),debug)
override config := Debug
endif
ifeq ($(config),release)
override config := Release
endif
ifeq ($(config),Debug)
CPPFLAGS ?= -DDEBUG
CFLAGS ?= -g -O0
CXXFLAGS ?= -g -O0
LDFLAGS ?= -g
else ifeq ($(config),Release)
CPPFLAGS ?= -DNDEBUG
CFLAGS ?= -O2
CXXFLAGS ?= -O2
else ifneq (,$(config))
$(warning Unknown configuration "$(config)")
endif

# Use "make RANLIB=''" for platforms without ranlib.
RANLIB ?= ranlib

CC := cc
CXX := c++

# The directory for the build files, may be overridden on make command line.
builddir = .

all: $(builddir)/mqtt_client $(builddir)/mqtt_publisher

$(builddir)/mqtt_client: $(builddir)/mqtt_client_laik_ext.o $(builddir)/mqtt_client_laik_mqtt.o $(builddir)/mqtt_client_laik_intf.o $(builddir)/mqtt_client_mqttclient.o
	$(CXX) -o $@ $(LDFLAGS) $(builddir)/mqtt_client_laik_ext.o $(builddir)/mqtt_client_laik_mqtt.o $(builddir)/mqtt_client_laik_intf.o $(builddir)/mqtt_client_mqttclient.o -lmosquitto -lprotobuf-c -pthread
	rm -f *.o
	rm -f *.d

$(builddir)/mqtt_client_laik_ext.o: proto/laik_ext.pb-c.c
	$(CC) -c -o $@ $(CPPFLAGS) $(CFLAGS) -MD -MP -pthread -I. proto/laik_ext.pb-c.c

$(builddir)/mqtt_client_laik_mqtt.o: test/laik_mqtt.c
	$(CC) -c -o $@ $(CPPFLAGS) $(CFLAGS) -MD -MP -pthread -I. test/laik_mqtt.c

$(builddir)/mqtt_client_laik_intf.o: laik_intf.c
	$(CC) -c -o $@ $(CPPFLAGS) $(CFLAGS) -MD -MP -pthread -I. laik_intf.c

$(builddir)/mqtt_client_mqttclient.o: mqttclient.c
	$(CC) -c -o $@ $(CPPFLAGS) $(CFLAGS) -MD -MP -pthread -I. mqttclient.c

$(builddir)/mqtt_publisher: $(builddir)/mqtt_publisher_laik_ext.o $(builddir)/mqtt_publisher_mqtt_publisher.o $(builddir)/mqtt_publisher_laik_intf.o $(builddir)/mqtt_publisher_mqttclient.o
	$(CXX) -o $@ $(LDFLAGS) $(builddir)/mqtt_publisher_laik_ext.o $(builddir)/mqtt_publisher_mqtt_publisher.o $(builddir)/mqtt_publisher_laik_intf.o $(builddir)/mqtt_publisher_mqttclient.o -lmosquitto -lprotobuf-c -pthread
	rm -f *.o
	rm -f *.d

$(builddir)/mqtt_publisher_laik_ext.o: proto/laik_ext.pb-c.c
	$(CC) -c -o $@ $(CPPFLAGS) $(CFLAGS) -MD -MP -pthread -I. proto/laik_ext.pb-c.c

$(builddir)/mqtt_publisher_mqtt_publisher.o: test/mqtt_publisher.c
	$(CC) -c -o $@ $(CPPFLAGS) $(CFLAGS) -MD -MP -pthread -I. test/mqtt_publisher.c

$(builddir)/mqtt_publisher_laik_intf.o: laik_intf.c
	$(CC) -c -o $@ $(CPPFLAGS) $(CFLAGS) -MD -MP -pthread -I. laik_intf.c

$(builddir)/mqtt_publisher_mqttclient.o: mqttclient.c
	$(CC) -c -o $@ $(CPPFLAGS) $(CFLAGS) -MD -MP -pthread -I. mqttclient.c

clean:
	rm -f *.o
	rm -f *.d
	rm -f $(builddir)/mqtt_client
	rm -f $(builddir)/mqtt_publisher

.PHONY: all clean

# Dependencies tracking:
-include *.d
